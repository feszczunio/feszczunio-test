/**
 * An array of custom build blocks to be applied on top of core ones.
 * Define block components to be added on a project level.
 */
const blocks = [
  // An array of all blocks available within this project.
];

export default blocks;
